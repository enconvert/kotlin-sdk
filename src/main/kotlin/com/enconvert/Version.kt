/** SDK version, in its own file so [Enconvert] can build its default User-Agent from it. */

package com.enconvert

/** SDK version string. Keep in sync with `version` in build.gradle.kts at release. */
public const val VERSION: String = "0.2.0"
